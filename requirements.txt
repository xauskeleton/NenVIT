# Cai torch/torchvision theo dung CUDA cua may truoc (xem README), roi:
timm>=1.0.27
numpy>=2.0
tqdm>=4.67
pillow

# Tuy chon
# datasets>=2.0     # chi can cho --dataset tiny (Tiny ImageNet qua HuggingFace)
# wandb             # chi can cho scripts/backfill_wandb.py
